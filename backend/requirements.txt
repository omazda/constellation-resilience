# Нижние границы на время разработки; перед сдачей зафиксировать точные версии
# (pip freeze) — воспроизводимость сборки оценивается отдельным критерием.
fastapi>=0.115
uvicorn[standard]>=0.30
numpy>=2.0

# тесты
pytest>=8.0
