"""Веб-слой: FastAPI, эндпоинт `/ws`, раздача собранного фронтенда и `/health`.

Слой тонкий: принимает сообщения конверта `{id, type, payload}`, валидирует вход, зовёт
`core/` и отдаёт ответ с тем же `id`. Расчётов здесь нет — вся математика в `core/`.
Контракт сообщений описан в `docs/PROTOCOL.md` и `docs/PROTOCOL.md`.
"""
