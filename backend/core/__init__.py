"""Расчётное ядро: орбиты, видимость, межспутниковые связи, маршруты, показатели.

Единственное правило этого пакета: здесь нет ни одного импорта из веб-слоя (`app/`).
Ядро принимает сценарий и числа, возвращает числа, и запускается без FastAPI —
это то, что проверяется критерием «Качество кода» и что позволяет сверяться с эталоном.

`geometry.py` — побайтовая копия официального «Расчетный модуль/geometry.py» из комплекта
задачи. Копия нужна, потому что Docker-образ собирается только из `backend/`. Формулы в ней
не трогаем: расхождение с оригиналом означает расхождение с эталоном `tools/golden.py`.
Своя логика живёт в соседних модулях и вызывает `geometry` как есть.
"""
